@page "{time:int=10}"
@model RimionshipServer.Pages.Api.Laufbanner

@{
    Layout = null;
}

<!DOCTYPE html>
@{
    var seconds = int.Parse(RouteData.Values["time"]!.ToString()!);
}
<html>
<head>
@{
#if DEBUG
}
    <link rel="stylesheet" href="/css/fonts.css">
@{
#else
}
    <link rel="stylesheet" href="/css/fonts.min.css">
@{
#endif
}
    <style>
    	.marquee {
    		max-width: 100vw;
    		white-space: nowrap;
    		overflow: hidden;
    		font-weight: 200;
    		font-size: xx-large;
    		color: #9d9d9c;
    		font-family: 'Roboto Condensed', sans-serif;
    	}
        
    	.marquee span {
    		display: inline-block;
    		padding-left: 100%;
    		animation: marquee @(Model.Entries.Count() * seconds)s linear infinite;
    	}
        
    	.marquee span div {
			 display: inline;
			 padding-right: 24px;
			 word-spacing: -3px;
    	}
        
    	.marquee span div b {
    		font-weight: 800;
    	}
    
    	@@keyframes marquee {
    	    0%   {transform: translate3d(0,0,0);}
    	    100% {transform: translate3d(-100%,0,0);}
    	}
    </style>

    <title></title>
</head>
<body>
<div>
    <div id="marquee" class="marquee">
		 <span>
        @{
            void Player(int pos, string name, double points) { <div>#@pos @name <b>@points</b></div> }
            foreach (var entry in Model.Entries)
                Player(entry.Item1 + 1, entry.Item2, entry.Item3);
				Player(1, "Brrainz", 34595);
				Player(2, "ArcticBathtue", 34595);
				Player(3, "schemfil", 1034595);
				Player(4, "Brrainz", 3455);
				Player(5, "Brrainz", 595);
				Player(6, "Whatever", 45534595);
        }
		  </span>
    </div>
</div>
<script type="text/javascript" defer="">
    window.setTimeout(() => {window.location = window.location}, @(Model.Entries.Count() * seconds * 1000));
</script>
</body>
</html>